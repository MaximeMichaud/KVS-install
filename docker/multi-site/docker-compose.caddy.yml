# =============================================================================
# KVS Multi-Site - Caddy Reverse Proxy
# =============================================================================
#
# This is the central reverse proxy that handles:
# - SSL certificates (automatic Let's Encrypt)
# - Routing to individual KVS sites
# - HTTP to HTTPS redirect
#
# USAGE:
#   ./site-manager.sh caddy-start
#
# The manager creates the external kvs-proxy network before Compose starts.
#
# Each KVS site connects to the 'kvs-proxy' network and Caddy routes
# traffic based on the domain name.
#
# =============================================================================

services:
  caddy:
    image: caddy:2-alpine
    container_name: kvs-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"  # HTTP/3 QUIC
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ./caddy/sites:/etc/caddy/sites:ro
      - caddy-data:/data
      - caddy-config:/config
    networks:
      - kvs-proxy
    environment:
      - ACME_EMAIL=${ACME_EMAIL:-admin@example.com}
    healthcheck:
      test: ["CMD", "caddy", "validate", "--config", "/etc/caddy/Caddyfile"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  kvs-proxy:
    name: kvs-proxy
    external: true

volumes:
  caddy-data:
  caddy-config:
